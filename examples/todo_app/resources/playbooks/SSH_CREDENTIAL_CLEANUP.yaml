---
- name: Cleanup SSH credentials placeholder
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No-op SSH credential cleanup
      debug:
        msg: "TODO app has no temporary SSH credentials to clean up."
