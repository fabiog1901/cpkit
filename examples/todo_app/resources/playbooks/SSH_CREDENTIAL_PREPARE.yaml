---
- name: Prepare SSH credentials placeholder
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No-op SSH credential preparation
      debug:
        msg: "TODO app does not require temporary SSH credentials."
