# cpkit Tools

Small framework-oriented helper scripts and examples live here.

| File | Purpose |
| --- | --- |
| `client.sh` | Example API-key request signing with HMAC-SHA256 using OpenSSL. |
| `codemap.py` | Project-agnostic deterministic code map generator used by `make codemap-write`. |
